/**
 * @roi-dealer/schemas — placeholder (PHASE 00).
 *
 * Implementation is scheduled for: PHASE 01 — Core Domain (далее расширяется в каждой Phase).
 * Do not add logic here before that phase is approved by the owner.
 */
export const PACKAGE_NAME = '@roi-dealer/schemas';
