{
  "name": "@roi-dealer/api",
  "version": "0.0.0",
  "private": true,
  "description": "ROI Dealer HTTP API (health endpoint; database health check from PHASE 02)",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch --conditions=@roi-dealer/source --env-file-if-exists=../../.env src/main.ts",
    "start": "node --enable-source-maps dist/main.js"
  },
  "dependencies": {
    "@roi-dealer/database": "workspace:*",
    "@roi-dealer/observability": "workspace:*",
    "@roi-dealer/shared": "workspace:*",
    "zod": "^4.6.5"
  }
}
